apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nginxagro-deployment
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/Adegokee/nginxagro-deployment.git'
    targetRevision: HEAD
    path: base
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: tunde
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
